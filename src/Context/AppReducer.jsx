export default (state , action) => {
    switch(action.type) {
        // case "DELETE_TRANSACTION" : 
        //     return {
        //         ...state , transactions: state.trnasactions.filter(transaction => transaction.id != payload)
        //     }
        default : 
            return state;
    }
}